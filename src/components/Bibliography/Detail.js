import React from 'react';

import FilterTable from './FilterTable';

const BibliographyDetail = ({ ensgId, symbol, sources }) => {
  return <React.Fragment>Bibliography placeholder</React.Fragment>;
};

export default BibliographyDetail;
