query SubcellularLocation($ensemblId: String!) {
  target(ensemblId: $ensemblId) {
    id
    subcellularLocations {
      source
      location
      termSL
    }
    proteinIds {
      id
      source
    }
  }
}
