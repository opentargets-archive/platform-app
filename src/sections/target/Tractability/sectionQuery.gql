query TargetTractabilitySectionQuery($ensgId: String!) {
  target(ensemblId: $ensgId) {
    id
    tractability {
      smallmolecule {
        buckets
      }
      antibody {
        buckets
      }
      otherModalities {
        buckets
      }
    }
  }
}
