import React from 'react';

const Section = ({ ensgId, efoId, target, disease, data }) => (
  <React.Fragment>
    TODO: Write Section component for <strong>{target.symbol}</strong> and{' '}
    <strong>{disease.name}</strong>.
  </React.Fragment>
);

export default Section;
