query ProteinInfoSectionQuery($ensgId: String!) {
  target(ensemblId: $ensgId) {
    id
    proteinAnnotations {
      id
      accessions
      functions
      pathways
      similarities
      subcellularLocations
      subunits
    }
  }
}
