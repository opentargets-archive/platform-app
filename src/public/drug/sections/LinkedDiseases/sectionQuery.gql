query LinkedDiseasesSectionQuery($chemblId: String!) {
  drug(chemblId: $chemblId) {
    id
    linkedDiseases {
      rows {
        id
        name
        therapeuticAreas {
          id
          name
        }
      }
    }
  }
}
